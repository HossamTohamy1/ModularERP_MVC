@model ModulerERP_MVC_.Areas.Finance.ViewModels.TreasuryListViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@{
    ViewData["Title"] = "الخزائن";
}

<div class="container-fluid">
    <!-- Header -->
    <div class="row mb-4 align-items-center">
        <div class="col-md-6">
            <h2 class="mb-0">
                <i class="fas fa-money-bill-wave text-primary me-2"></i>
                @ViewData["Title"]
            </h2>
            <p class="text-muted mb-0">إدارة خزائن الشركة</p>
        </div>
        <div class="col-md-6 text-end">
            <a asp-action="Create" class="btn btn-primary">
                <i class="fas fa-plus me-2"></i>إضافة خزينة جديدة
            </a>
        </div>
    </div>

    <!-- Alerts -->
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
            <i class="fas fa-check-circle me-2"></i>
            <strong>نجاح!</strong> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert">
            <i class="fas fa-exclamation-circle me-2"></i>
            <strong>خطأ!</strong> @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- Filters Card -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-light">
            <h6 class="mb-0">
                <i class="fas fa-filter me-2"></i>
                البحث والتصفية
            </h6>
        </div>
        <div class="card-body">
            <form asp-action="Index" method="get" class="row g-3">
                <div class="col-md-5">
                    <label class="form-label fw-bold">
                        <i class="fas fa-search me-1"></i>بحث
                    </label>
                    <input type="text"
                           name="searchTerm"
                           value="@Model.SearchTerm"
                           class="form-control"
                           placeholder="ابحث عن خزينة بالاسم أو الوصف...">
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-bold">
                        <i class="fas fa-toggle-on me-1"></i>الحالة
                    </label>
                    <select name="status" class="form-select">
                        <option value="">الكل</option>
                        <option value="0" selected="@(Model.FilterStatus == ModularERP.Common.Enum.Finance_Enum.TreasuryStatus.Active)">نشط</option>
                        <option value="1" selected="@(Model.FilterStatus == ModularERP.Common.Enum.Finance_Enum.TreasuryStatus.Inactive)">غير نشط</option>
                    </select>
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="fas fa-search me-1"></i>بحث
                    </button>
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <a asp-action="Index" class="btn btn-outline-secondary w-100">
                        <i class="fas fa-redo me-1"></i>إعادة تعيين
                    </a>
                </div>
            </form>
        </div>
    </div>

    <!-- Treasuries Table Card -->
    <div class="card shadow-sm">
        <div class="card-header bg-white">
            <h6 class="mb-0">
                <i class="fas fa-list me-2"></i>
                قائمة الخزائن
                @if (Model.Treasuries.Any())
                {
                    <span class="badge bg-primary rounded-pill">@Model.Treasuries.Count()</span>
                }
            </h6>
        </div>
        <div class="card-body p-0">
            @if (Model.Treasuries.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th class="ps-3">
                                    <i class="fas fa-signature me-1"></i>اسم الخزينة
                                </th>
                                <th>
                                    <i class="fas fa-building me-1"></i>الشركة
                                </th>
                                <th>
                                    <i class="fas fa-money-bill me-1"></i>العملة
                                </th>
                                <th>
                                    <i class="fas fa-wallet me-1"></i>الرصيد
                                </th>
                                <th>
                                    <i class="fas fa-info-circle me-1"></i>الحالة
                                </th>
                                <th>
                                    <i class="fas fa-calendar me-1"></i>تاريخ الإنشاء
                                </th>
                                <th class="text-center">
                                    <i class="fas fa-cog me-1"></i>الإجراءات
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Treasuries)
                            {
                                <tr>
                                    <td class="ps-3">
                                        <div>
                                            <strong class="d-block">@item.Name</strong>
                                            @if (!string.IsNullOrEmpty(item.Description))
                                            {
                                                <small class="text-muted">
                                                    <i class="fas fa-info-circle me-1"></i>@item.Description
                                                </small>
                                            }
                                        </div>
                                    </td>
                                    <td>
                                        <i class="fas fa-building text-muted me-1"></i>
                                        @item.CompanyName
                                    </td>
                                    <td>
                                        <span class="badge bg-info bg-opacity-10 text-info border border-info">
                                            @item.CurrencyCode
                                        </span>
                                    </td>
                                    <td>
                                        <strong class="@(item.Balance >= 0 ? "text-success" : "text-danger")">
                                            <i class="fas @(item.Balance >= 0 ? "fa-arrow-up" : "fa-arrow-down") me-1"></i>
                                            @item.Balance.ToString("N2") @item.CurrencySymbol
                                        </strong>
                                    </td>
                                    <td>
                                        @if (item.Status == ModularERP.Common.Enum.Finance_Enum.TreasuryStatus.Active)
                                        {
                                            <span class="badge bg-success">
                                                <i class="fas fa-check-circle me-1"></i>نشط
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">
                                                <i class="fas fa-times-circle me-1"></i>غير نشط
                                            </span>
                                        }
                                    </td>
                                    <td>
                                        <small class="text-muted">
                                            <i class="far fa-calendar-alt me-1"></i>
                                            @item.CreatedAt.ToString("dd/MM/yyyy")
                                        </small>
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a asp-action="Details"
                                               asp-route-id="@item.Id"
                                               class="btn btn-outline-info"
                                               title="التفاصيل"
                                               data-bs-toggle="tooltip">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a asp-action="Balance"
                                               asp-route-id="@item.Id"
                                               class="btn btn-outline-success"
                                               title="الرصيد"
                                               data-bs-toggle="tooltip">
                                                <i class="fas fa-wallet"></i>
                                            </a>
                                            <a asp-action="Edit"
                                               asp-route-id="@item.Id"
                                               class="btn btn-outline-warning"
                                               title="تعديل"
                                               data-bs-toggle="tooltip">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a asp-action="Delete"
                                               asp-route-id="@item.Id"
                                               class="btn btn-outline-danger"
                                               title="حذف"
                                               data-bs-toggle="tooltip">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <div class="mb-4">
                        <i class="fas fa-inbox fa-4x text-muted opacity-50"></i>
                    </div>
                    <h5 class="text-muted mb-3">لا توجد خزائن متاحة</h5>
                    <p class="text-muted mb-4">ابدأ بإضافة خزينة جديدة لإدارة أموال شركتك</p>
                    <a asp-action="Create" class="btn btn-primary btn-lg">
                        <i class="fas fa-plus me-2"></i>إضافة خزينة جديدة
                    </a>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Auto-hide alerts after 5 seconds
            setTimeout(function() {
                $('.alert').fadeOut('slow');
            }, 5000);

            // Initialize tooltips
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl);
            });
        });
    </script>
}